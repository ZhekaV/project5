version: '2'
services:
  # db:
  #   image: postgres
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/project5
    ports:
      - "3000:3000"
      - "5432:5432"
    # depends_on:
    #   - db
